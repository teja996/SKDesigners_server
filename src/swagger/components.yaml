components:
  schemas:
    # Schemas from products.yaml
    $ref: './products.yaml#/components/schemas'

    # Schemas from categories.yaml
    $ref: './categories.yaml#/components/schemas'

    # Schemas from users.yaml
    $ref: './users.yaml#/components/schemas'

    # Schemas from orders.yaml
    $ref: './orders.yaml#/components/schemas'

  securitySchemes:
    # Security schemes from users.yaml and orders.yaml
    $ref: './users.yaml#/components/securitySchemes'
